# Data Directory

This directory contains the NYC Taxi Trip Dataset files.

## File Structure

```
data/
├── raw/                          # Raw dataset files (place your files here)
│   ├── train.zip                 # Official NYC Taxi Trip Dataset
│   ├── train.csv                 # Extracted CSV file (after unzipping)
│   └── taxi+_zone_lookup.csv     # Taxi Zone Lookup Table (if available)
├── processed/                    # Cleaned and processed data
│   ├── cleaned_trips.csv
│   └── processing_log.json
└── README.md                     # This file
```

## Instructions

1. **Place your train.zip file here**: `/data/raw/train.zip`
2. **Extract the CSV**: Unzip train.zip to get train.csv
3. **Taxi Zone Lookup**: If you have the zone lookup table, place it as `taxi+_zone_lookup.csv`
4. **Run processing**: Use the updated data processing script

## Dataset Information

The official NYC Taxi Trip Dataset contains:
- Pickup and drop-off timestamps
- Pickup and drop-off coordinates (longitude/latitude)
- Trip duration and distance
- Fare amount and tip
- Passenger and payment metadata
- Vendor information

## Processing

The data processing pipeline will:
1. Load the raw CSV data
2. Clean and validate the data
3. Calculate derived features
4. Load into the database
5. Generate processing logs

